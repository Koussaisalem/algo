# Locked dependency versions that mirror the dev container

# Core tensor + GNN stack
torch==2.5.1
torch-geometric==2.6.1
nequip==0.15.0

# Chemistry + numerical tooling
rdkit==2025.3.6
numpy==1.26.4

# Testing
pytest==9.0.2

# Optional tooling (install manually if needed)
# xtb-python  # QM driver used during dataset enrichment; available via conda-forge
# torch-cluster
# torch-scatter
# torch-sparse
# torch-spline-conv