"""Top-level package for the QCMD hybrid framework.

This package hosts data preparation utilities, NequIP-based models,
and manifold-aware diffusion tooling that extend the verified
`qcmd_ecs` core without modifying it directly.
"""
